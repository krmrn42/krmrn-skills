{
  "$schema": "https://platform.claude.com/schemas/marketplace.json",
  "name": "krmrn-skills",
  "description": "A community-friendly marketplace of Claude Code skills and plugins — opinionated, evidence-based, and progressively-disclosed. Contributions welcome.",
  "owner": {
    "name": "krmrn42"
  },
  "metadata": {
    "pluginRoot": "./plugins"
  },
  "plugins": [
    {
      "name": "skill-linting",
      "source": "./plugins/skill-linting",
      "description": "Hard structural lint for Claude Code skills. Zero-deps Python script (scripts/lint.py) reports tier-1 errors (frontmatter validity, name/description length caps, name format, parent-dir match, body line cap, manifest version-sync, plugin registration) and tier-2 warnings (third-person heuristic, exclusion clause, reference TOC threshold, reference depth, orphan references). Three output formats: human, GitHub Actions annotations, json. Honors per-line noqa comments and a .skill-lint.toml config for repo-wide and per-skill disables (with a reason convention). Slash command /skill-linting:lint, Makefile target make lint-skills, pre-commit hook. Not for general markdown linting, code formatting, or content review.",
      "version": "0.1.0",
      "author": {
        "name": "krmrn42"
      },
      "category": "engineering",
      "keywords": [
        "skill-linting",
        "lint",
        "validation",
        "ci",
        "pre-commit",
        "python",
        "skills",
        "plugins",
        "marketplace",
        "claude-code",
        "frontmatter",
        "structural-checks"
      ]
    },
    {
      "name": "chat-search",
      "source": "./plugins/chat-search",
      "description": "Cross-project full-text search across local Claude Code conversations. Maintains its own SQLite FTS5 index at $XDG_DATA_HOME/krmrn42-skills/chat-search/index.db, refreshed lazily from ~/.claude/projects/**/*.jsonl on every startup — so search hits every conversation on disk, not just what Claude Code's index remembers. Three surfaces: one-shot CLI (`ccsearch <query>` — ranked list with project, date, msg count, snippet, copy-paste resume one-liner per row); built-in TUI picker (`ccsearch -i` — Enter resumes in the original project dir, Ctrl-F forks, Ctrl-O prints id, Ctrl-D prints path); `/chat-search:find` inline rendering. Zero external deps — one Node script using node:sqlite (Node 22.5+); no fzf, no Python. `/chat-search:setup` symlinks `bin/ccsearch` into ~/.local/bin. Flags: `--regex`, `--scan`, `--include-tools`, `--only-user`, `--project`, `--since`, `--limit`, `--reindex`, `--index-status`. Trigger on \"find that chat where I figured out X\", \"search history for Y\".",
      "version": "0.4.0",
      "author": {
        "name": "krmrn42"
      },
      "category": "engineering",
      "keywords": [
        "chat-search",
        "search",
        "fts",
        "fts5",
        "sqlite",
        "conversation",
        "history",
        "resume",
        "claude-code",
        "cli",
        "regex",
        "bm25",
        "node-sqlite"
      ]
    }
  ]
}
