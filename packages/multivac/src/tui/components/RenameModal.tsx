import React from "react";
import { Box, Text } from "ink";

export function RenameModal() {
  return (
    <Box>
      <Text dimColor>Enter save   Esc cancel   Ctrl-U clear   (empty + Enter clears the saved name)</Text>
    </Box>
  );
}
