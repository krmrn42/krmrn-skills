## 1. Pre-flight (one-time setup)

- [x] 1.1 Register the `@krmrn42` scope on npm (run `npm org create krmrn42` or the equivalent `npm access` flow under the publishing account) and confirm it is reserved.
- [x] 1.2 Generate an `NPM_TOKEN` with publish scope; add as `NPM_TOKEN` repository secret in GitHub Actions.
- [x] 1.3 Confirm Node 22.5+ is installed in the local dev environment (`node -v`); the test script and `npm pack` both need it. (verified: `node -v` → `v25.9.0`)

## 2. Monorepo skeleton + source rename

- [x] 2.1 Create `packages/multivac/` with subdirectories `src/` and `test/`.
- [x] 2.2 `git mv plugins/chat-search/bin/ccsearch packages/multivac/src/multivac.js` (rename to the new bin name with a `.js` extension so node tooling treats it as a module).
- [x] 2.3 `git mv plugins/chat-search/bin/indexer.js packages/multivac/src/indexer.js`.
- [x] 2.4 `git mv plugins/chat-search/bin/picker.js packages/multivac/src/picker.js`.
- [x] 2.5 `git mv plugins/chat-search/bin/ccsearch.test.sh packages/multivac/test/multivac.test.sh`.
- [x] 2.6 Inside `packages/multivac/src/multivac.js`, update any `require('./indexer.js')` / `require('./picker.js')` paths if they referenced the old `bin/` location; helper-module filenames are unchanged so most paths should still resolve. (verified: requires are bare `./indexer.js` / `./picker.js` — node's realpath-based resolution finds them next to the moved entry script.)
- [x] 2.7 Update `packages/multivac/test/multivac.test.sh` to invoke the renamed entry script (`packages/multivac/src/multivac.js`) and to assert against the new program name in help output (see task 4.4 for the help-banner cases). (path retarget + `$CCSEARCH` → `$MULTIVAC` rename done; `CCSEARCH_DB` env var preserved for the audit in 4.3.)
- [x] 2.8 Create relative symlinks in the plugin's `bin/`: `ln -s ../../../packages/multivac/src/multivac.js plugins/chat-search/bin/multivac` and parallel symlinks for `indexer.js` and `picker.js`. Commit the symlinks. Do NOT create a `plugins/chat-search/bin/ccsearch` symlink — the historical name is retired.
- [x] 2.9 Verify `realpath plugins/chat-search/bin/multivac` resolves into `packages/multivac/src/multivac.js`, and that `plugins/chat-search/bin/multivac --help` runs end-to-end. (verified: `realpath` resolves cleanly; `--help` prints the banner — banner still says `ccsearch` since section 4 hasn't run yet.)

## 3. Package authoring

- [x] 3.1 Write `packages/multivac/package.json` with: `name: "@krmrn42/multivac"`, `version: "0.6.0"`, `description`, `license: "MIT"`, `author: { name: "krmrn42" }`, `repository: { type: "git", url: "https://github.com/krmrn42/krmrn-skills.git", directory: "packages/multivac" }`, `bugs.url: "https://github.com/krmrn42/krmrn-skills/issues"`, `homepage`, `engines: { node: ">=22.5.0" }`, `bin: { multivac: "src/multivac.js" }`, `files: ["src/", "README.md", "LICENSE"]`. Do NOT add a `ccsearch` bin alias. Omit `dependencies` and `peerDependencies` (or set them to `{}`). (also added `keywords` for npm discoverability.)
- [x] 3.2 Write `packages/multivac/README.md` (npm-facing, Claude-Code-specific phrasing per the deferred genericization question): short overview, install commands (`npm install -g @krmrn42/multivac` + `multivac init`, and the `npx -y @krmrn42/multivac --help` quick-start), link to `plugins/chat-search/MANUAL.md` via a stable GitHub URL for the full user guide.
- [x] 3.3 Add `packages/multivac/LICENSE` (MIT, matching the existing plugin license).
- [x] 3.4 Set executable bit on `packages/multivac/src/multivac.js` (`chmod +x`); confirm git tracks the mode. (verified: mode is `755`, preserved across `git mv`.)

## 4. CLI changes — program-name banner

- [x] 4.1 In `packages/multivac/src/multivac.js`, locate `buildHelp()` and replace the hardcoded literal `"ccsearch"` in the synopsis with the hardcoded literal `"multivac"` (per design D4 — no dynamic argv-basename detection).
- [x] 4.2 Audit all of `buildHelp()` and the `Examples:` block for remaining `ccsearch` references; replace with `multivac`.
- [x] 4.3 Grep the rest of `packages/multivac/src/` for any `ccsearch` string literal that surfaces to the user (error messages, log lines, file-path defaults, banner text); replace with `multivac` where the binary name is meant. Leave the historical name only in code comments that explain the rename or in the test script's "rename smoke test" cases. (done: ccsearch → multivac, CCSEARCH_DB → MULTIVAC_DB, CCSEARCH_TEST → MULTIVAC_TEST in both source and test. User explicitly chose hard rename for CCSEARCH_DB. Final `grep -c ccsearch packages/multivac/src/` returns 0.)
- [x] 4.4 Update `packages/multivac/test/multivac.test.sh` with positive cases: invoke `multivac --help` and assert `usage: multivac` appears in stdout, and assert `ccsearch` does NOT appear anywhere in `--help` output. (also added `assert_not_contains` helper; T53/T54 paths fixed to point at the plugin's MANUAL/README, which is where their structural invariants live.)
- [x] 4.5 **Added during apply**: implement `--version` flag — short-circuits in `parseArgs` by reading `require("../package.json").version`. Registered in OPTIONS so the drift-guard T26 passes. Added to the synopsis line. Resolves the gap surfaced by task 12.2 (`multivac --version`).

## 5. CLI changes — `init` subcommand

- [x] 5.1 Add an early-positional check in the argv parser: if `argv[0]` (after flag stripping) equals the literal `init`, route to `runInit()`; otherwise proceed with the existing flow. (implementation: dispatch lives in `main()` after `validateArgs()`, gated by `args.query === "init" && !args.literalQuery`.)
- [x] 5.2 Implement `runInit()`: probe `command -v claude` via `child_process.spawnSync`; if absent, print the "Manual install" section with both slash commands and exit 0. (CLAUDE-DETECT done via $PATH-walk in `claudeOnPath()` — no shell, no DEP0190. Manual-install text uses the **terse wording** chosen via AskUserQuestion: heading "Claude Code (`claude`) not on $PATH.", one-line "To install manually, paste into a Claude Code session:", then the two slash commands unindented for copy-paste.)
- [x] 5.3 If `claude` is on PATH, run the equivalent of `/plugin marketplace add krmrn42/krmrn-skills` then `/plugin install chat-search@krmrn-skills` in sequence, streaming stdout/stderr to the user; on either non-zero exit, exit 2 with the underlying error surfaced. (**Initially shipped wrong (D2 misread):** I called `claude '/plugin marketplace add …'` as a single slash-command argv, which Claude Code rejects with "/plugin isn't available in this environment." + exit 0 — silent failure caught during 12.5 by the user. **Fixed:** now uses `claude plugin marketplace add krmrn42/krmrn-skills` and `claude plugin install chat-search@krmrn-skills` — the real subcommand surface from `claude plugin --help`. Verified via the new T59 test using a shim claude that asserts the constructed argv shape.)
- [x] 5.4 Detect "already configured" by parsing `claude` output (best-effort) — if either subcommand reports the marketplace/plugin is already installed, surface that as success (exit 0) rather than a duplicate-install error. (heuristic chosen via AskUserQuestion: `/already\b/i` against combined stdout+stderr. Required `runClaudeSubcommand` to switch from `stdio:'inherit'` → `stdio:['inherit','pipe','pipe']` + replay so we *can* inspect the strings. New unit test T58 exercises 8 cases: null-safe, plain-failure, three "already X" wordings, case-insensitivity, and word-boundary discrimination (`alreadyEnabled` does NOT match). The `Idempotent re-invocation` spec scenario is now satisfied for claude outputs containing the word "already".)
- [x] 5.5 Document `init` in `buildHelp()`: add a "Subcommands" section listing `init`, and `Examples:` lines for `multivac init` and the `--` literal-query escape (`multivac -- init`). (synopsis updated to `[init | query]`; new `Subcommands:` block between positional and Options; 2 Examples lines.)
- [x] 5.6 Implement the `--` end-of-options sentinel: when `argv` contains `--` as a positional token, treat the rest as a query (so users can search for the literal token `init`). (already present in parseArgs; added `args.literalQuery = true` flag so `main()` can disambiguate from the subcommand path.)
- [x] 5.7 Add test cases in `multivac.test.sh` for: `multivac init` with `claude` absent (mock by overriding `PATH=/usr/bin`), `multivac -- init` parses as a query, and `multivac init --no-such-flag` exits 1. (3 new tests T55/T56/T57; all passing.)

## 6. NAMING.md relocation

- [x] 6.1 `git mv plugins/chat-search/NAMING.md packages/multivac/NAMING.md`. (file was untracked at the old path; landed as `A packages/multivac/NAMING.md` in git status.)
- [x] 6.2 Decide: either delete the old path entirely OR leave a one-line stub at `plugins/chat-search/NAMING.md` saying "Moved to [`packages/multivac/NAMING.md`](../../packages/multivac/NAMING.md)." — recommend the stub for backlink stability. (no stub: verified no backlinks elsewhere in repo. False positives in `plugins/prdspec/` are about a different `flag-naming.md`.)
- [x] 6.3 Update any internal links in `packages/multivac/NAMING.md` that referenced relative paths inside `plugins/chat-search/` (the doc's "References" section links to README/MANUAL relatively); rewrite them to the new relative root (`../../plugins/chat-search/README.md` etc).

## 7. Slash-command rewrites

- [x] 7.1 Rewrite `plugins/chat-search/commands/find.md`: replace every `ccsearch` token (in code blocks, prose, and Examples) with `multivac`. Update the fallback note ("If the bare-name invocation returns `ccsearch: command not found`...") to use `multivac: command not found`. (replace_all; final `grep ccsearch find.md` returns nothing.)
- [x] 7.2 Rewrite `plugins/chat-search/commands/setup.md`: change `SRC="${CLAUDE_PLUGIN_ROOT}/bin/ccsearch"` to `${CLAUDE_PLUGIN_ROOT}/bin/multivac`; change `DEST="$TARGET_DIR/ccsearch"` to `$TARGET_DIR/multivac`; update all human-readable output strings; update the "Done — `ccsearch` is on your PATH" message family to use `multivac`. (replace_all + targeted edit to add the cleanup step to the "What this command does" list.)
- [x] 7.3 Add the stale-`ccsearch`-symlink cleanup step to `setup.md`'s bash block (per design D8): before symlinking the new `multivac`, check whether `$TARGET_DIR/ccsearch` exists as a symlink whose `readlink` resolves into a path containing `plugins/chat-search/bin/`; if so, `unlink` it and `echo` a one-line notice ("removed stale `ccsearch` symlink from prior install"). A regular file or a symlink resolving elsewhere is left in place with a one-line warning surfaced to the user. (added a `LEGACY_CC` block right after `mkdir -p`. Pattern match: `case "$legacy_target" in *plugins/chat-search/bin/*) ... esac` — captures both absolute and relative readlink targets.)
- [x] 7.4 Update the slash command's frontmatter `description` field (the trigger surface Claude reads to decide auto-invocation) to mention `multivac` rather than `ccsearch`. (setup.md frontmatter: ccsearch → multivac via replace_all. find.md frontmatter never said ccsearch — left unchanged for trigger neutrality.)

## 8. Plugin manifest + marketplace updates

- [x] 8.1 Update `plugins/chat-search/.claude-plugin/plugin.json`: bump `version` from `0.5.0` to `0.6.0`; rewrite `description` to use `multivac` everywhere `ccsearch` appeared, and add a phrase noting the npm install path (`npm install -g @krmrn42/multivac && multivac init`). Keep the paragraph within the length cap enforced by `skill-linting`. (note: confirmed `DESCRIPTION_MAX=1024` in lint only applies to SKILL.md, NOT plugin.json — the existing description was 1089 chars and shipped fine. New description is 1151 chars; well within practical bounds.)
- [x] 8.2 Update `.claude-plugin/marketplace.json`: bump the `chat-search` entry's `version` to `0.6.0`; update the entry's `description` to match the new `plugin.json` description verbatim (the existing repo convention).
- [x] 8.3 Add a CHANGELOG entry (top of `plugins/chat-search/README.md` or a new `plugins/chat-search/CHANGELOG.md` — match repo convention) that explicitly calls out the rename: "**0.6.0 — `ccsearch` → `multivac`.** The on-PATH binary is now `multivac`. Users who ran `/chat-search:setup` previously must re-run it; the command will clean up the stale `~/.local/bin/ccsearch` symlink automatically. Also installable as `npm install -g @krmrn42/multivac`." (new file `plugins/chat-search/CHANGELOG.md` — repo had zero CHANGELOG precedent, so this is the first. Keep-a-Changelog format; 0.6.0 entry covers the binary rename, env var rename, npm distribution, --version, and -- escape.)

## 9. Lint extension — version-sync check

- [x] 9.1 Locate the existing lint script (e.g., `scripts/lint.py` per the existing `skill-linting` plugin description) and add a new check that reads `packages/multivac/package.json#version`, `plugins/chat-search/.claude-plugin/plugin.json#version`, and the matching marketplace entry's `version`. (per your choice, extended `check_marketplace()` in lint.py. Mapping is a `PLUGIN_NPM_PACKAGE_DIRS = {"chat-search": "multivac"}` constant — hard-coded for now; add an entry when another plugin grows an npm sibling. Skipped silently when `packages/<dir>/package.json` doesn't exist.)
- [x] 9.2 Emit a tier-1 error if any pair disagrees; the error message MUST name all three files and their values. (rule id: `marketplace.plugin.package-version-sync`. Error names all three paths + values verbatim, e.g. "packages/multivac/package.json='0.7.0', plugins/chat-search/.claude-plugin/plugin.json='0.6.0', .claude-plugin/marketplace.json#chat-search='0.6.0'".)
- [x] 9.3 Add a unit test for the lint check covering both equal and skewed states. (new file `plugins/skill-linting/scripts/test_lint.py`, stdlib `unittest` only — keeps zero-deps posture. 4 cases: equal versions → no finding; package skewed → exactly one finding with all three paths; plugin skewed → both legacy and new rules fire; no `package.json` → check silently skipped. All 4 pass.)
- [x] 9.4 Run `make lint-skills-strict`; confirm it passes against the change-as-shipped. (`make ci` runs lint + unit tests, exit 0. Manually verified the skew-detection path by temporarily bumping packages/multivac/package.json to 0.7.0, observing the expected tier-1 error, restoring.)
- [x] 9.5 **Added during apply**: new `make test-lint` Makefile target + extend `make ci` to include it. Keeps the new test discoverable via `make help` and ensures CI runs both lint and unit tests.

## 10. Plugin-side documentation

- [x] 10.1 Update `plugins/chat-search/README.md`: add an "Install via npm" subsection alongside the existing `/plugin install` instructions; cross-link from both directions. Replace every `ccsearch` token with `multivac` in user-facing prose. (replace_all 41→0 occurrences. Added "### Via npm" subsection under "## Install" with `npm install -g` and `npx -y` flows + `multivac init` explainer. Cross-link to CHANGELOG.md. Also fixed two unrelated typos in install block: marketplace name was `krmrn42-skills` instead of `krmrn-skills`, and the local-dev path leaked to the public README.)
- [x] 10.2 Update `plugins/chat-search/MANUAL.md`: global rename `ccsearch` → `multivac` in invocation examples; add a "How is it distributed?" subsection explaining that the marketplace plugin and the npm package are the same code; note that the rename was a hard cut at `0.6.0` and the `/chat-search:setup` command cleans up stale symlinks. (replace_all 40→0 occurrences. New `## How it's distributed` section inserted between Troubleshooting and Compatibility, plus matching TOC update. T53 structural test updated to expect 12 sections in the new order — verified passing.)
- [x] 10.3 Update top-level repo `CLAUDE.md` (the "What this repo is" section): mention `packages/multivac/` and the dual-distribution shape so future Claude sessions don't trip on the new layout. (rewrote opening paragraph and the plugins list. Added new "## Dual-distribution shape (multivac)" section explaining canonical sources + version-sync lint.)
- [x] 10.4 If a top-level `README.md` exists, add a "Monorepo layout" section; otherwise rely on `CLAUDE.md`. (top-level README.md exists — updated the chat-search row in Plugins table, added prdspec row, rewrote the Layout tree to show `packages/multivac/` and the symlink relationship. Plus an explanatory paragraph after the tree.)

## 11. CI — publish workflow

- [x] 11.1 Create `.github/workflows/publish-multivac.yml`: trigger on `push` to tags matching `multivac-v*`. (created. Security-hardened: no `${{ github.event.* }}` interpolation in `run:` blocks; tag version read via `$GITHUB_REF` env var only.)
- [x] 11.2 Workflow step 1: checkout the repo at the tag. (`actions/checkout@v4`.)
- [x] 11.3 Workflow step 2: setup Node 22.5+. (`actions/setup-node@v4` with `node-version: "22"` and `registry-url: https://registry.npmjs.org` for npm-auth wiring.)
- [x] 11.4 Workflow step 3: run `make lint-skills-strict` (fails the publish on any lint error, including version skew). (added as a step + a precursor step that asserts tag-version == package.json version, so a mistagged release also fails before npm sees it. Also added a `make test-lint` step so the unit tests run in CI.)
- [x] 11.5 Workflow step 4: `cd packages/multivac && npm publish --access public` using `NPM_TOKEN`. (uses `working-directory: packages/multivac` + `NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`.)
- [x] 11.6 Add a job-status badge to `plugins/chat-search/README.md` (optional but recommended). (added two: workflow status + npm version badge for `@krmrn42/multivac`. Both will show "no status" / "v0.6.0" once the first publish lands.)

## 12. End-to-end verification

- [x] 12.1 Local dry-run: `cd packages/multivac && npm pack --dry-run` — inspect the file list; confirm `src/`, `README.md`, `LICENSE` are present and `test/`, `*.test.sh`, `NAMING.md`, OpenSpec files are absent. (verified: tarball ships exactly 6 files — LICENSE, README.md, package.json, src/indexer.js, src/multivac.js, src/picker.js. 34.1 kB packaged, 113.4 kB unpacked. Test/, NAMING.md, openspec/ all correctly excluded.)
- [x] 12.2 Local pack-install: `npm pack`, then in a clean directory `npm install -g ./krmrn42-multivac-*.tgz`, then run `multivac --version`, `multivac --help` (verify banner says `multivac`), and verify `command -v ccsearch` returns nothing. (verified via `npm install --global --prefix <tmpdir>`. Results: `multivac --version` → `0.6.0`; `--help` banner starts `usage: multivac [-h] [--version] ...`; zero `ccsearch` literals in `--help`; no `ccsearch` bin shipped.)
- [ ] 12.3 In-repo plugin sanity: from a Claude Code session with the marketplace pointed at the working tree, run `/chat-search:find sometext` and confirm it returns results (i.e., the plugin's symlinked `bin/multivac` resolves and runs). **YOUR TASK** — requires a live Claude Code session, can't be done from this CLI agent context.
- [x] 12.4 Stale-symlink cleanup: create `~/.local/bin/ccsearch` as a symlink into `plugins/chat-search/bin/` (mimicking a prior install), run `/chat-search:setup`, and verify the stale symlink is removed and a new `~/.local/bin/multivac` symlink is created. (verified the cleanup *logic* by extracting the bash block from setup.md and running it against 3 scenarios: stale plugin-owned symlink → REMOVED; symlink pointing elsewhere → PRESERVED; regular file → PRESERVED. The end-to-end run through `/chat-search:setup` itself is `12.3`-adjacent and requires a live Claude Code session.)
- [ ] 12.5 `multivac init` happy-path: in a clean VM or container with the `claude` CLI installed but no marketplace added, run `multivac init` and confirm both `plugin marketplace add` and `plugin install` succeed. **READY FOR RE-RUN** — first attempt found the silent-failure bug (slash-command vs subcommand). After the fix, the new T59 test asserts the right argv shape via a `claude` shim, and the docker test (12.6) confirms the broader install/help/fallback flow works in a clean container. The final happy-path with a real `claude` binary that has no krmrn42 marketplace yet is the last thing you can validate in your live env.
- [x] 12.6 `multivac init` fallback: in a container with no `claude` CLI on PATH, run `multivac init` and verify the manual-install slash commands are printed and exit is 0. (verified in `node:22-slim` docker container with `--network none`. Confirmed: install via `npm install -g /tmp/pkg.tgz`, `multivac --version` → `0.6.0`, `multivac --help` shows new banner with `[init | query]`, `multivac init` prints the two slash commands + exits 0. One harmless side-observation: Node 22.22.3 prints an `ExperimentalWarning: SQLite is an experimental feature` — this is a Node ecosystem notice, not a multivac issue.)
- [ ] 12.7 `npx` zero-install: in a clean directory on a machine with Node 22.5+ that has never seen the package, run `npx -y @krmrn42/multivac --help` after the first publish lands and confirm it works. **YOUR TASK** — requires the first publish to land first.

## 13. Release

- [ ] 13.1 Open a PR with all of the above; ensure CI passes including the new version-sync lint.
- [ ] 13.2 Merge to `main`.
- [ ] 13.3 Tag `multivac-v0.6.0`; push the tag; watch the publish workflow run.
- [ ] 13.4 Verify the package appears on npm; confirm the README renders correctly on npmjs.com; run `npx -y @krmrn42/multivac --help` from a fresh machine.
- [ ] 13.5 Announce the rename and the npm path in the appropriate channel (CHANGELOG entry per task 8.3, social/announcement post — author's call).
